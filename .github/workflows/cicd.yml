name: Hello World Workflow

on:
  push:
    branches:
      - medium

jobs:
  say-hello:
    runs-on: [custom-runner, X64, ubuntu, amd]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "Hello, World! GitHub Actions is working 🎉"
